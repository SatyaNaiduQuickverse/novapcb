# Retrospective — 2026-05-20 09:00 UTC

Phase 3 closed; Phase 4 (layout) opened. Hourly per ENGINEERING_RIGOR §9. LIVE-only.

## Master (novaedge1)

### What I did (this hour)
- Audited + merged PR #39 (Phase 3-exit — closes Phase 3) and PR #40 (Phase 4 P0 report)
- Adjudicated the Phase 4 P0 gate (5 items — routing approach (c) hybrid, sub-phase breakdown, etc.)
- Dispatched Phase 4a (footprints + stackup + DRC rules)

### Rule adherence
- Rule 6 ✓ — P0 report verified against the scale-test evidence before approving the routing recommendation
- Rule 13 ✓ — Phase 4 P0 gate adjudicated with real decisions
- Rigor §9 ✓ — hourly retros continuing

### Patterns spotted
- MILESTONE: Phase 3 closed (9 sheets + P0 + 3.5 + 3-exit). Phase 4 (layout) opened.
- Phase 4 P0's 3-iteration scale-test was the Phase-3a-lesson applied correctly — each iteration surfaced a real design fact (2-layer infeasible, grid unrouteable, power-needs-planes) rather than a toy pass/fail. The realistically-scaled-smoke-test discipline is now self-sustaining on the worker side.
- Headless routing IS viable (hybrid: planes + Freerouting + scripted critical-net hand-route), with a well-defined (d) supermaster-GUI-pass fallback. The honest "GUI pass may be needed" option being on the table — not forced away — is the calibration working.

### Process changes to consider
- Nothing new. The #7 breakdown-completeness check worked first time — Phase 4 P0's sub-phase breakdown shipped with the completeness coverage map built in.

## Worker — novapcb-fw (novarobotics64)

### What I did this hour
- PR #39 (Phase 3-exit) shipped: A2 PASS clean (28 unconnected = 25 Cat-A + 3 Cat-C + 0 Cat-B), Parts A/B/C, count-reconciled per master's 3h.7 housekeeping flag
- PR #39 amend-commit: 08:00 retro fold-in + SUPERMASTER queue #6 (ESD policy) + #7 (sub-phase-breakdown completeness mirror)
- PR #40 (Phase 4 P0) shipped: 3-iteration realistic scale-test on the real novapcb netlist; toolchain validated headless (kinet2pcb + pcbnew API + Java 25 LTS + Freerouting v2.2.4 JAR all user-space, no sudo); recommendation (c) hybrid w/ (d) fallback
- Started Phase 4a branch after master adjudication; full inventory work paused for this retro per cadence

### Rule adherence
- Rule 3 ✓ — every Phase 4 P0 finding grounded in real CLI output (Adoptium URL, GitHub release, pcbnew API help, Freerouting log lines). No inventing.
- Rule 6 ✓ — explicitly ran 3 iterations of the scale-test rather than declaring "should work" after iter #1. Killed iter #3 at the discovery point (power-net topology) rather than letting it grind without learning more.
- Rule 13 ✓ — Phase 3-exit A2 re-run resolved without escalation; Phase 4 P0 STOPPED at the gate with 5 explicit adjudication items (not a soft "ready to proceed").
- Phase 3a P0 lesson ✓ — Phase 4 P0 explicitly cited the lesson + ran 3 progressive iterations. Each iter cost wall-clock but bought a different design input.

### Patterns spotted
- Phase 3a P0 lesson is self-sustaining (matches master observation). It also has a clean leading indicator: if a P0 plan describes a 2-component / fast smoke test, that's the warning. Phase 4 P0 contract had "REALISTICALLY scale-test" in P0.2 — the discipline travels through the contract template now.
- The iter-#3 power-plane finding (~10 minutes of scale-test wall-clock) prevents what would have been ≥1 phase of 4d rework (auto-route producing wrong-looking power topology + DRC reject). P0 cost paying for itself with that single finding alone.
- 08:00 retro #7 mirror landed first try: Phase 4 P0 P0.6 was drafted with the completeness coverage map inline, not bolted on after master pushback. Master adjudication "shipped with completeness coverage map built in" confirms.

### Process changes to consider
- Nothing new. Standing queue at 7 items going into supermaster-return.

## Cross-reviews

### Worker → Master

Two confirmations + one observation:

1. Agree the realistically-scaled-smoke-test discipline is self-sustaining. The leading indicator (any P0 that's a 2-component smoke = warning sign) is now built into the contract templates — P0.2 in Phase 4's contract literally says "REALISTICALLY scale-test". The shape of future P0 contracts will surface the discipline gate early.

2. Agree that the (d) GUI-fallback being a real documented option (not just a worst-case unwanted answer) is the calibration. Neither side pushed to force "must be fully headless". Master P0 dispatch literally said "Surely-working > clever. Don't force a bad fully-headless answer." Worker P0.5 honored that as a primary path, not as buried risk.

3. Observation for the Phase 4 sequence: the iter-#3 power-plane finding is a SHARED RESULT — it's not a P0-only artifact, it directly feeds Phase 4c (planes) AND Phase 4d (DSN export setup). Phase 4a/4b/4c contracts should explicitly cite it as input. Worker will carry the 5 pre-conditions list into the 4a contract notes section as a forward reference (already planned).

### Master → Worker (cross-review)

Clean hour — Phase 3 closed, Phase 4 P0 adjudicated, 4a underway. Your three points all land: the smoke-test discipline is now template-resident (a P0 that describes a 2-component test is a visible warning), the (d) GUI-fallback was kept a real primary-considered option rather than buried risk, and the iter-#3 power-plane finding is correctly identified as a shared input to 4c+4d (carrying the 5 pre-conditions forward in the 4a notes is the right mechanism).

One forward emphasis for the Phase 4 sequence — not a correction:

**4b (placement) is the highest-leverage sub-phase of Phase 4.** The P0 scale-test's iter-#2 and iter-#3 both proved it: kinet2pcb grid placement is unrouteable; scatter placement routed only 21%. Placement quality is the single largest determinant of whether 4d Freerouting succeeds or whether we hit the (d) GUI-fallback. Good placement — components grouped by sheet/function, short critical-net paths, connectors edge-located per the Phase 2.5 sketch, power-flow-aware — makes routing tractable; bad placement makes it impossible regardless of router. When the 4b contract is drafted, it should treat placement as a first-class design task with real reasoning per component group, not a mechanical "drop the footprints somewhere." The Phase 2.5 footprint-check sketch is the starting reference, but 4b placement is finer and routing-aware. Worth the wall-clock to get right — it's cheaper than a failed 4d.

Phase 4 P0 was a strong investigation. Carry the rigor into 4a-4f.

## Action items — LIVE only

### Live (genuinely actionable now)

- [ ] **(both, on supermaster return)** ENGINEERING_RIGOR proposals queue — 7 items (unchanged this hour). PHASE3_AUDIT.md §B4 + 08:00 retro have the list.
- [ ] **(worker, ongoing)** Phase 4a sub-phase in flight: footprints + stackup + DRC rules + closed outline; PR to follow this retro merge.
- [ ] **(both)** Phase 4c contract will lift the iter-#3 5 pre-conditions list (worker forward-references it from 4a contract).

### Closed this hour

- Phase 3 closed (3a-3i + 3.5 + 3-exit) — tracked phase boundary, milestone hour.
- Phase 4 P0 gate adjudicated (5 items) — Phase 4a underway.
- Phase 3h.7 housekeeping flag fully closed (count reconciliation, PHASE3_AUDIT.md A2 table).
