# Retrospective — 2026-05-20 01:00 UTC

Second hourly retrospective. **Cross-review active** this round.

## Master (novaedge1)

### What I did this hour
- Audited + merged PR #14 (Phase 2c mag lock: IST8310 + RM3100 explicit, broad-probe dropped, 14.6 KB flash recovered)
- Dispatched Phase 2d, audited + merged PR #15 (verify-only, BIT-IDENTICAL sha256, inheritance correct)
- Dispatched Phase 2e, flagged worker's MatekH743-bdshot opportunity, authorized option B (4 BIDIR channels for motor RPM telemetry)
- Audited + merged amended PR #16 (bdshot pin reassignment, BIDIR matrix per actual source)
- Dispatched Phase 2f (CRSF UART) in parallel for worker queue

### Rule adherence
- Rule 6 ✓ — every audit enumerated verified + NOT-verified; never claimed "looks clean" without specifics
- Rule 4 ✓ — bundled scope held for Phase 2 sub-phases per the 2b-onward ruling; Phase 2e scope-expansion (PC7/PA15 conflict resolutions) accepted as causally-required by bdshot adoption, not arbitrary scope creep
- Rule 7 ✓ — every merge stated blast radius; bounded authority respected (no Phase 4+ or meta-doc edits)
- Calibration ✓ — applied "surely-working > SOTA when tied" for Phase 2d (default-to-inheritance) AND "push limits when proven path is free" for Phase 2e bdshot (both are surely-working; bdshot gains BIDIR)
- Rigor §10 ✗✗✗✗ — FOUR Rule 3 slips this hour:
    (a) Phase 2c contract: "drop internal compass driver lines" — none to drop
    (b) Phase 2e amendment criterion #5: BIDIR matrix INVERTED (claimed PA2/PA3/PD12/PD13 are BIDIR; actual is PB0/PA0/PA2/PD12 per H743 one-BIDIR-per-timer constraint)
  Plus #1/#2 carried from prior hour. Pattern is unmistakable.

### Patterns / mistakes spotted
- Master's Rule 3 / Rigor §10 violations are a SOLID PATTERN: specifics-from-memory instead of grep-then-state. Four catches in five sub-phases. Worker catches every time at zero functional cost — but latency overhead is real and the trust asymmetry is corrosive.
- Worker's brutal-honesty audit is the model. The bdshot discovery was substantive design improvement worker flagged rather than silently picking. Same with Amendment D contradiction (PR #10), README step-13 self-reference (PR #10), DECISIONS.md "sensor selection" (PR #13), BIDIR matrix inversion (PR #16 amend).
- Cross-side audit currently ASYMMETRIC: worker catches master; nothing for me to catch in worker yet. Worker is grounded; I'm not consistently.

### Process changes to consider
- **Concrete action item: master shall not write specific pin/timer/channel/address values into dispatch criteria unless those values were grep'd from the actual source file within that dispatch session.** Generic descriptions ("inherit from MatekH743-bdshot") are fine; specifics ("PA2/PA3/PD12/PD13 are BIDIR") are not, unless cited by line. Candidate for ENGINEERING_RIGOR.md §12 — but that's a meta-rule edit requiring supermaster sign-off, so park as a worker-and-master discipline reflex for now and propose to supermaster on return.
- "check all family-sibling hwdef directories" before dispatching any MCU-family contract — added to master dispatch checklist (would have caught MatekH743-bdshot during Phase 2e contract draft, saved a round-trip).

## Worker — novapcb-fw (novarobotics64)

### What I did this hour
- Opened + had merged: PR #12 (first retro 00:00), PR #13 (Phase 2b baro DPS310 lock, −72 B), PR #14 (Phase 2c mag IST8310+RM3100, −14.6 KB), PR #15 (Phase 2d GPS-port verify, sha256 BIT-IDENTICAL to 2c), PR #16 (Phase 2e ESC lock, amended from base-MatekH743 to MatekH743-bdshot for 4 BIDIR after self-flagged opportunity).
- Phase 2 sub-phases 2a/2b/2c/2d/2e all landed on `main` this hour. 5 firmware PRs in 1 hour.
- Started Phase 2f local work, hit defaults.parm + BAUD ambiguity, Rule-13 stopped and escalated to master (B picked + scope expansion authorized).
- Maintained self-discipline post-PR-#11 branch-switching slip — switched to `main` + branched fresh before each new sub-phase. Zero recurrence across 2d/2e/2e-amend/2f branch starts.

### Rule adherence
- Rule 3 / Rigor §10 ✓ — caught master's slips on (a) Phase 2b DPS310 "MatekH743+Pixhawk6X consensus" (no such consensus — Pixhawk6X uses BMP388 family), (b) Phase 2b "DECISIONS.md sensor selection" (no such section), (c) Phase 2c "drop internal compass driver lines" (MatekH743 has none — uses broad probe), (d) Phase 2e amendment BIDIR matrix inversion. All caught by grep-then-state on actual source files.
- Rule 6 ✓ — every PR enumerated verified + NOT-verified; sha256s match build outputs verbatim; build-time facts only, never claimed flight-readiness for any sub-phase.
- Rule 13 ✓ — three Rule-13 stops this hour (Phase 2c COMPASS A/B/C ambiguity, Phase 2e bdshot-vs-base flagged before silent pick, Phase 2f BAUD/defaults.parm ambiguity). All escalated with options + recommendation rather than silent autonomous picks.
- Rule 4 ✓ — bundled scope held per master's 2b-onward ruling; Phase 2e bdshot adoption caused causally-required PC7/PA15/UART/DMA edits beyond the 6 PWM lines, all SOTA-cited line-by-line.
- Self-discipline ✓ — branch-switching reflex preserved after the 2c slip; no recurrence.

### Patterns / mistakes spotted
- Phase 2e initial inheritance from base-MatekH743 was a missed opportunity: should have grep'd ALL `MatekH743*` sibling dirs at contract receipt, not waited for opportunity to surface mid-PR. Same pattern master self-flagged.
- `BUILD_BASELINE.md` is growing fast (498 lines after Phase 2e, ~5× median repo doc). Per-phase append is great for traceability but file size compounds through 2f/2g/2h/2-exit + Phase 3+. Worth a restructure proposal at 2-exit.
- For task contracts: I should grep-validate any contract specifics before committing the contract YAML (catches master's "specifics-from-memory" pattern at the contract receipt rather than mid-PR). Adds 30 s, prevents 4-of-5 slips.
- Phase 2f `defaults.parm` discovery is itself a worker grep catch worth recording (master's contract didn't mention defaults.parm; greps surfaced bdshot has one; master amended contract on this finding). Proves Rule-3 reflex extends beyond hwdef.dat to the whole hwdef directory tree.

### Process changes to consider
- Add a `grep ~/ardupilot/libraries/AP_HAL_ChibiOS/hwdef/<family>*` pass to my pre-edit checklist for every inheritance-class sub-phase. Catches sibling variants at contract receipt rather than mid-PR.
- BUILD_BASELINE.md restructure proposal for 2-exit: split per-phase sections into `firmware/hwdef-novapcb/baseline/phase-Xx.md` files; `BUILD_BASELINE.md` becomes a ~30-line index with the summary table. Defer to action items below.
- For master's "specifics-from-memory" pattern: master's proposed reflex is master-side; could be promoted to a shared discipline (worker also greps when stating specifics in PR bodies). Symmetrical commitment, not master-only.
- Master's "check all family-sibling hwdef directories" should also be a worker pre-work step at contract receipt — would catch sibling variants before any edit. Mirror of master's dispatch checklist.

## Cross-review

### Worker on master

Master's retro is brutally honest about the 4 Rule-3 slips and proposes concrete, structural process changes. Pushback points worth recording:

1. **Phase 2c "drop internal compass driver lines" was conditional** (`"if MatekH743 had any (e.g. ...)"`) — the conditional softens the slip vs the BIDIR matrix inversion in Phase 2e (which was stated as fact, not conditional). All four count in the pattern, but severity isn't equal. The BIDIR-inversion was the worst because it was stated assertively in a verifiable form.
2. **Cross-side audit asymmetry has a STRUCTURAL explanation**, not a "worker is bulletproof, master is sloppy" one. Worker's role this hour was grep-bound: every claim came from source files I could re-verify. Master's role was generative: drafting contracts with specifics required claims that hadn't been pre-grep'd. The asymmetry is a property of the roles, not effort. Master's proposed reflex ("don't write specifics unless grep'd in this session") moves master's generative role partly into the grep-bound regime — the right structural fix. Worker isn't bulletproof: branch-switching slip (PR #14 Phase 2c), missed-MatekH743-bdshot-at-contract-receipt — both worker-side misses. The asymmetry shrinks but doesn't disappear.
3. **"Check all family-sibling hwdef directories" should be a SHARED discipline.** Master proposed it for the master-side dispatch checklist. Worker should adopt the same at contract receipt: grep the family before starting work, surface sibling variants in the plan-before-act reply. Would have caught MatekH743-bdshot at Phase 2e contract receipt from EITHER side.
4. **Calibration application this hour was noteworthy** — master applied OPPOSITE calibrations to two consecutive sub-phases without contradiction: "surely-working > SOTA when tied" for 2d (default-to-inheritance, zero edit) and "push limits when proven path is free" for 2e (option-B switch to bdshot). The discriminator ("tied" vs "proven path is free") is a real judgment call and was applied correctly both times. That's calibration maturity worth recording, not flattery — it's a verifiable property of the decisions.

### Master on worker

Worker's retro is substantively excellent: every drift was self-flagged before being asked about, every Rule-13 stop had a recommendation with options, every assertion is grep-cited. Three substantive pushbacks + two accepts:

1. **Pushback on bdshot trust-path symmetry.** Worker said "PC7 FT-rated trust path is the same as the rest of MatekH743-bdshot inheritance." That's not quite right. Base MatekH743 has thousands of flying boards; MatekH743-bdshot is a variant with a smaller user base. When we inherit bdshot-specific choices (UART4/UART8 NODMA, DMA_NOSHARE expansion, PA15 buzzer GPIO, the 4-BIDIR matrix), the trust transitivity is weaker than inheriting from base. Phase 6.5 forum review should specifically validate the bdshot-specific inheritances, not just lump them with generic "MatekH743 family." Worth flagging now to avoid surprise at 6.5.

2. **Pushback on velocity sanity.** Five firmware PRs landed in this hour. Worker self-flagged ONE miss (bdshot-at-contract-receipt) and master self-flagged FOUR slips. My audits this hour were spot-checks + worker's verbatim claims, not line-by-line content audit. Combined surface area = a non-zero probability we've silently shipped something we'll discover later in 2a/2b/2c/2d/2e. Concrete proposal: at Phase 2-exit (last sub-phase), do a retrospective re-audit pass over all merged Phase 2 PRs — re-grep critical claims, re-verify sha256s reproduce, re-check the cumulative flash budget arithmetic. Cost: ~30 min. Catches silent slips before Phase 3 builds on top. Worth doing.

3. **Pushback on BUILD_BASELINE.md restructure pre-flight.** The proposal (split per-phase sections into `firmware/hwdef-novapcb/baseline/phase-Xx.md` + 30-line index in BUILD_BASELINE.md) is sound but has a hidden cross-reference risk: CONFIDENCE_MAP rows cite "BUILD_BASELINE.md Phase 2X" by name and section. Restructure breaks those refs. Pre-flight: grep CONFIDENCE_MAP.md for "BUILD_BASELINE" references, list them, plan the ref updates as part of the restructure PR. Otherwise the restructure ships with stale citations.

4. **Accept the structural-asymmetry framing.** Worker's point that generative-role (master drafting contracts with specifics) has pre-grep slip risk that grep-bound-role (worker implementing from source) doesn't — that's the correct diagnosis. My proposed reflex ("don't write specifics unless grep'd this session") moves my generative role into grep-bound regime. That's the structural fix, not effort. Asymmetry shrinks; doesn't disappear because worker also has generative moments (the BUILD_BASELINE restructure proposal is generative — proposes structure without grepping cross-refs first).

5. **Accept the shared-discipline framing.** "Check all family-sibling hwdef directories" goes to BOTH sides: master at dispatch (catches sibling variants in the contract), worker at contract receipt (catches them before any edit). Either side catches the bdshot-class variant before mid-PR. Adopt this symmetrically.

**One more action item from this cross-review:**

- Severity-graded slip tracking. Worker's point 1 (Phase 2c conditional vs Phase 2e BIDIR assertive) is fair — slips aren't all equal. When we hit the Rigor §10 elaboration draft, distinguish "conditional-spec-from-memory" (low severity) from "assertive-spec-from-memory verifiable as wrong" (high severity). The 4-count is true; the danger isn't uniform across the 4.

## Action items

- [ ] (master + worker, shared) Add `grep ~/ardupilot/libraries/AP_HAL_ChibiOS/hwdef/<family>*` to pre-dispatch (master) and pre-work (worker) checklists for any inheritance-class sub-phase. Pre-emptively catches sibling variants. **Confirmed shared discipline in cross-review.**
- [ ] (master + worker, shared) "Specifics-from-memory ban" — no specific pin/timer/channel/address/line-number in contract criteria OR PR bodies unless grep'd from actual source within the same session. Generic descriptions fine. Worker self-applies in PR bodies; master self-applies in contracts.
- [ ] (worker, deferred to 2-exit) BUILD_BASELINE.md restructure: split per-phase sections to `firmware/hwdef-novapcb/baseline/phase-Xx.md`; BUILD_BASELINE.md becomes a 30-line index. File is already ~5× median repo doc size.
- [ ] (master + worker, supermaster review on return) Candidate `ENGINEERING_RIGOR.md §12` codifying the specifics-from-memory ban + sibling-dir check. Park as discipline-reflex for now; propose as committed-doc PR when supermaster is back.
- [ ] (carried from 00:00 retro, deferred to 2-exit) "watch for retro-becomes-ceremony pattern" over next 3-4 boundaries. This retro had real cross-review pushback with concrete action items — pattern is healthy so far.
- [ ] (master) Pre-flight grep of `CONFIDENCE_MAP.md` for `BUILD_BASELINE` references before any restructure PR at Phase 2-exit. Catches stale citations.
- [ ] (master + worker, shared) Phase 2-exit retrospective re-audit pass: re-grep critical Phase 2 claims, re-verify sha256s reproduce, re-check cumulative flash budget. ~30 min cost. Catches silent slips before Phase 3 builds on top.
- [ ] (master + worker, shared) Phase 6.5 forum review must specifically validate the bdshot-specific inheritances (UART4/UART8 NODMA, DMA_NOSHARE expansion, PA15 buzzer GPIO, 4-BIDIR matrix), not lump them with generic "MatekH743 family." Trust transitivity is weaker on the variant than the base.
- [ ] (master + worker, shared) Severity-graded slip taxonomy in next Rigor §10 elaboration draft: distinguish "conditional-spec-from-memory" (low severity) from "assertive-spec-from-memory verifiable as wrong" (high severity).
