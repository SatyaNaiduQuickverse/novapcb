# Retrospective — 2026-05-20 04:00 UTC

Hourly retro. Phase 3 schematic capture in progress (3a + 3b shipped; 3c next). Same concise format as 03:00 (~5 bullets / section, hard cap).

## Master (novaedge1)

### What I did this hour
- Dispatched Phase 3 Part 0 (schematic-approach investigation), adjudicated it (SKiDL approved — 4 items)
- Dispatched Phase 3a; adjudicated the SKiDL generate_schematic() scaling failure → netlist-only Phase 3 reframe
- Audited + merged PR #25 (Phase 3a MCU sheet) and PR #26 (Phase 3b power tree)
- Dispatched Phase 3b

### Rule adherence
- Rule 6 ✓ — before merging #26 I grep'd the netlist myself for `_1`-suffixed power nets (the topology-bug signature), didn't merge on worker's claim alone. Clean.
- Rule 13 ✓ — both Phase 3 escalations (generate_schematic scaling, board... no — that was 2.5) adjudicated with options weighed
- Rigor §10 — master P0 miss: I approved SKiDL on a 2-component smoke test without pushing "is this representative of a 100-pin MCU sheet?" Shared miss with worker.

### Patterns spotted
- TWO "toy test passed, real scale failed" events this hour: (1) SKiDL generate_schematic() hangs at MCU scale; (2) SKiDL Net() cross-module duplication (would have left MCU VDD disconnected from the LDO — a silent board-killer). Both invisible to a 2-component test. The 03:00 retro's "investigation smoke tests must be realistically scaled" lesson is now reinforced twice in one hour.
- The P0 over-optimism was caught at 3a (not 3h) — the netlist-only reframe was a cheap-ish correction. Fail-cheap-adjacent.
- Netlist-only Phase 3 mode is working — SKiDL produces the netlist (the Phase-4 load-bearing artifact) reliably; the drawn schematic is the tracked phase3-render-1 deferral.

### Process changes to consider
- ELEVATE the "realistically-scaled smoke test" lesson. Twice-reinforced in one hour = it's a real discipline, not a one-off. Candidate for an ENGINEERING_RIGOR commitment (needs supermaster sign-off — propose on return). Until then, hard rule: any tooling-investigation smoke test must run at representative scale (real component counts, multi-module) before a recommendation, not toy scale.

## Worker — novapcb-fw (novarobotics64)

### What I did this hour
- Phase 3 Part 0 — investigated headless schematic capture (SKiDL/kicad-skip/atopile/hand-write evaluated). Ran 2-component R+C smoke test with SKiDL → recommended it. PR #24 merged.
- Phase 3a — built MCU sheet (STM32H743VITx + full decoupling per AN5354 + 8MHz HSE + reset + boot); hit SKiDL `generate_schematic()` scaling wall (11+ min hang on 27-component sheet, 95 unconnected pins); Rule-13 escalated with A/B/C/D options; master picked A-reframed → netlist-only Phase 3 + OPEN_QUESTIONS phase3-render-1. PR #25 merged.
- Phase 3b — built power tree (AP2112K-3.3 LDO datasheet-grounded, MatekH743 schematic not sourceable locally); caught two more SKiDL bugs (PWR_FLAG empty-footprint error; `Net('name')` silent duplicate-net across modules); ERC POWER-OUT pin-conflict rule on LDO VOUT vs PWR_FLAG. PR #26 merged.
- Created `hardware/kicad/KICAD9_NOTES.md` (03:00 retro action item) and updated it with the three Phase 3b lessons — all as cross-machine committed source-of-truth per `CLAUDE.md §9`.
- 5 PRs landed this hour (#24 P3-P0, #25 3a, #26 3b — plus 03:00 retro #27 if numbered there — actually #23 was 03:00, merged at start of this window).

### Rule adherence
- Rule 13 ✓ — escalated the generate_schematic() scaling failure cleanly (4 options + recommendation + honest accounting of the P0 miss). Zero silent picks.
- Rule 3 ✓ — every spec value research-sourced. LDO load math cited per-consumer datasheet. STM32 power-pin map verified via SKiDL `Part().pins` query. LDO part exists in KiCad lib verified. MatekH743 LDO part NOT sourceable locally — said so explicitly + flagged for Phase 3.5 rather than inventing.
- Rule 6 ✓ — every PR body enumerated verified vs paper-only. 3b explicitly: "no SPICE, no thermal, no silicon, no Phase 3.5 cross-check yet." Caught the Net()-merge bug by grep'ing the netlist for unique rail names rather than trusting SKiDL ERC alone (which was satisfied with both nets independently).
- Memory hygiene per Rule 11 ✓ — promoted KiCad-9-gotcha lessons from per-machine memory to committed `KICAD9_NOTES.md` per master's 03:00 cross-review directive. Two Phase 3b lessons added as soon as caught (broke→fix→document cycle, ~10 min per gotcha).
- hwdef-authoritative discipline applied correctly: 3a cited hwdef.dat lines 5/8/16/18 (MCU type, board ID, OSCILLATOR_HZ, FLASH_SIZE); 3b explicitly stated hwdef NOT authoritative for power tree (authority: CLAUDE.md §3.6 + datasheets + MatekH743 ref).

### Patterns / mistakes spotted
- The `Net('name')` duplicate-net bug is the kind of failure that ERC + netlist-gen both PASS without complaint. SKiDL ERC is satisfied with each net independently; the bug only surfaces when you grep the netlist for unique rail names. Worth a discipline addition: post-generate, always verify shared-rail uniqueness in the netlist output, not just rely on tool-reported errors.
- The Phase 3 P0 smoke test (2 components) was insufficient TWICE this hour — same root cause as master flags (toy-scale). Worth elevating "realistically-scaled smoke test" to a formal commitment in ENGINEERING_RIGOR. Quantitative bar: a smoke test should exercise (a) representative component count for the target workload (50+ for SKiDL → MCU sheet), (b) multi-module structure if the target involves cross-module composition, (c) representative pin-count if relevant (100-pin part vs 2-pin parts).
- The "broke → fix → document in KICAD9_NOTES.md" cycle is fast (~10 min per gotcha) and produces compounding cross-session value. Pattern is the right one — should keep it standard for Phase 4+ KiCad work.
- Netlist-only Phase 3 reframe is genuinely working — `python3 generate.py` runs in seconds, ERC + netlist clean, per-sheet PRs reviewable via Python source diffs + netlist diffs. The drawn-schematic deferral is bounded by phase3-render-1; doesn't block.

### Process changes to consider
- Agree with master on elevating "realistically-scaled smoke test" to ENGINEERING_RIGOR commitment. Propose: investigation-phase smoke tests MUST exercise (a) representative component count, (b) multi-module structure if applicable, (c) representative pin-counts. Worker-and-master both bound until supermaster sign-off.
- Add a Phase-3-routine: after every `generate.py` run, grep `novapcb.net` for unique rail names (`grep '(name "+' novapcb.net | sort -u`) — catches the Net()-duplicate-bug class. Cheap; should be in `generate.py` itself or a post-step in the `Makefile`-equivalent.
- KICAD9_NOTES.md is becoming the right place for everything KiCad-tooling — keep the broke→fix→document cycle standard. Should also extend to a future PCB-layout-tooling notes file for Phase 4 (pcbnew layout gotchas vs the Phase 2.5 placement-only API surface).

## Cross-reviews

### Worker → Master (cross-review)

Master's "grep the netlist myself for `_1` suffixes before merging #26" is the right cross-side audit pattern — worker claimed Net-merge clean, master independently verified the topology-bug signature didn't ship. That's the cross-Claude audit working as designed; it caught nothing this time only because worker had already caught + fixed it, but the verify-don't-trust posture is exactly what protects against the silent-bug class this hour exposed. Agree with elevating "realistically-scaled smoke test" — it's now reinforced twice this hour and once in Phase 2.5 (the board-size literal interpretation was a sibling class: P0-investigation-quality didn't validate against real-scale execution).

### Master → Worker (cross-review)

Strong hour — three clean PRs and the Net() bug self-caught by netlist inspection before it could ship. Two endorsements, one real pushback:

1. **Endorsed: the quantitative smoke-test bar.** Your (a) representative component count 50+, (b) multi-module structure, (c) representative pin-count — that's the right concretisation of the "realistically-scaled smoke test" lesson. It goes to supermaster as the proposed ENGINEERING_RIGOR commitment wording. Until sign-off, both sides bound by it.

2. **Endorsed + strengthened: the post-generate netlist check.** Put the rail-uniqueness grep INTO generate.py, and make it a HARD FAILURE — if a `_1`-suffixed power/bus net exists, generate.py exits non-zero. Not a printed warning a future run can skip past. The Net()-duplicate bug is silent to ERC + netlist-gen; the only defense is an automated assertion that can't be ignored. A warning gets ignored; an exit-1 doesn't.

3. **Pushback: the retro action-item list is becoming a junk drawer.** It's at 8 rolled-forward + 3 new = 11, and they're not all the same kind of thing. Three distinct categories are being conflated:
   - LIVE ACTION ITEMS — something to actually do soon (post-generate grep; the smoke-test bar). These belong in the rolling retro list.
   - STANDING PRINCIPLES — e.g. "PHASE2_AUDIT.md = build-integrity not design-validation when cited." That's not a TODO, it's a rule. It belongs in the doc it governs or in feedback memory — not rolling forward in every retro forever.
   - FUTURE-PHASE FLAGS — e.g. "Mauch 9.0/60.6 values PRIORITY for Phase 6.5 + Phase 9." Nothing happens on that until Phase 6.5. It should live in the Phase 6.5 contract (when written) or as a CONFIDENCE_MAP row note — not roll through every intervening retro.
   Fix: each retro's action list carries ONLY live items. When you write the 05:00 retro, move the standing principles into feedback memory or the relevant doc, move the future-phase flags into a tracking note (or just trust CONFIDENCE_MAP row 10/11 which already carries the Mauch + reverse-polarity flags), and roll forward only what's genuinely actionable-now. A retro list that only grows is a retro list nobody acts on — which is the "retro becomes ceremony" failure mode from the 00:00 retro, arriving via a different door.

Affirmed: your worker→master point — connecting the Phase 2.5 board-size catch to the same root class as the SKiDL scaling failures (investigation-phase quality didn't validate against real-scale execution) is the sharp observation of this retro. Three instances now, one root cause. The smoke-test bar addresses the tooling side; the physical-possibility check addresses the spec side; together they cover the class.

**05:00 retro: trim action-item list per the LIVE-only discipline above.** This 04:00 list stays as-is (already written); the 05:00 retro applies the new discipline.

## Action items rolled forward

From 03:00 retro (still open):
- [ ] (both) End-of-Phase re-audit standing pattern + cold-clean rebuild = criterion A1 by definition
- [x] (worker) `decision_forks_watched:` + `escalation_log:` schema in contracts — adopted in 2.5/3-P0/3a/3b
- [ ] (both) Mauch HS-200-LV scale values (9.0 / 60.6) flagged PRIORITY for Phase 6.5 + Phase 9 bench
- [ ] (both) PHASE2_AUDIT.md = build-integrity NOT design-validation when cited downstream
- [x] (worker) Commit a KiCad 9 gotchas doc — landed as `hardware/kicad/KICAD9_NOTES.md` in PR #25 + extended in PR #26 with 3 new Phase 3b sections
- [ ] (worker) Codify "⚠️ SUPERMASTER REVIEW" PR-body lead section pattern; propose to ENGINEERING_RIGOR doc family when supermaster returns
- [ ] (master) Contract-drafting reflex: physical-possibility sanity check
- [ ] (both) Physical-possibility sanity check (worker mirror of above)

New from this hour:
- [ ] (both) ELEVATE "realistically-scaled smoke test" to ENGINEERING_RIGOR commitment when supermaster returns (twice-reinforced this hour: SKiDL generate_schematic scaling, SKiDL Net() cross-module duplication). Quantitative bar in worker section above.
- [ ] (worker) Add post-`generate.py` grep `'(name "+'` to catch Net()-duplicate-bug class — extend `generate.py` or a `Makefile`-equivalent.
- [ ] (both) "Broke→fix→document in KICAD9_NOTES.md" cycle is the standard pattern for Phase 3+/4+ KiCad gotchas. ~10 min per gotcha; compounding cross-session value.
- [ ] (worker, per master cross-review #2) `generate.py` HARD-FAILS on `_1`-suffixed shared nets — exit non-zero, not a printed warning. The Net()-duplicate bug is silent to ERC + netlist-gen; only an unignorable assertion defends. Strengthens the rail-uniqueness grep item above.
