# Phase 6k — see PHASE6_PLAN.md + PHASE6_P0_RESULTS.md

Run: `python3 run_6k.py` (with `LD_LIBRARY_PATH=~/local/ngspice/usr/lib/aarch64-linux-gnu` if PySpice-using).

Outputs:
- `results.json` — structured per-check pass/fail
- `plots/` — generated plots (if applicable)
- `results.md` — human readout (where present)
